# ===================================================================
# JHipster Sample Spring Cloud Config.
# ===================================================================

# Property used on app startup to check the config server status
configserver:
    name: Greashoppe Registry config server
    status: Connected to the JHipster Registry config server, using https://github.com/DivisoSofttech/platform-configurations !

# Default JWT secret token (to be changed in production!)
#jhipster.security.authentication.jwt.base64-secret
jhipster:
    security:
        authentication:
            jwt:
                secret: LRIHwqdiRUUGOeXX54RjnVts1vRYxkr5E4DUwq70IBc
